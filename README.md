# 📊 SalesAggregator — Агрегатор данных о продажах

**Лабораторная работа №3** — Обработка и агрегация CSV-данных о продажах

---

## 👨‍🎓 Информация о студенте

- **ФИО:** _[Укажите ваше ФИО]_
- **Группа:** _[Укажите вашу группу]_
- **Вариант:** 5
- **Дата:** _[Укажите дату]_

---

## 🛠 Технологический стек

- **Язык программирования:** Java 11+ (рекомендуется Java 17)
- **Система сборки:** Gradle 8.x
- **IDE (рекомендуется):** IntelliJ IDEA
- **Формат данных:** CSV (Comma-Separated Values)
- **Конфигурация:** Properties-файлы

---

## 📁 Структура проекта

```
SalesAggregator/
├── src/
│   └── main/
│       └── java/
│           └── com/salesaggregator/
│               ├── Main.java              # Главный класс приложения
│               ├── SalesAggregator.java   # Логика агрегации данных
│               └── model/
│                   └── Sale.java          # Модель данных о продаже
├── data/
│   └── sales.csv                          # Входные данные (CSV-файл)
├── app.properties                         # Конфигурационный файл
├── build.gradle                           # Конфигурация сборки Gradle
├── settings.gradle                        # Настройки проекта
├── gradlew                                # Gradle wrapper для Linux/macOS
├── gradlew.bat                            # Gradle wrapper для Windows
├── Запуск.bat                             # Скрипт быстрого запуска (Windows)
├── Запуск.command                         # Скрипт быстрого запуска (macOS/Linux)
└── README.md                              # Документация проекта
```

---

## 🚀 Пошаговые инструкции по запуску

### Вариант 1: Запуск в Windows (через командную строку)

#### Предварительные требования:
- Установлена Java JDK 11 или выше
- Проверьте установку: откройте `cmd` и выполните `java -version`

#### Шаги запуска:

1. **Откройте командную строку (cmd)** или PowerShell
   - Нажмите `Win + R`, введите `cmd` и нажмите Enter

2. **Перейдите в директорию проекта:**
   ```cmd
   cd путь\к\проекту\SalesAggregator
   ```

3. **Выполните сборку проекта:**
   ```cmd
   gradlew.bat build
   ```
   
4. **Запустите приложение:**
   ```cmd
   gradlew.bat run
   ```

#### ✨ Альтернативный способ (один клик):
Просто дважды щелкните на файл **`Запуск.bat`** в корне проекта!

---

### Вариант 2: Запуск в macOS/Linux (через терминал)

#### Предварительные требования:
- Установлена Java JDK 11 или выше
- Проверьте установку: откройте терминал и выполните `java -version`

#### Шаги запуска:

1. **Откройте терминал**
   - macOS: `Cmd + Space`, введите "Terminal"
   - Linux: `Ctrl + Alt + T`

2. **Перейдите в директорию проекта:**
   ```bash
   cd путь/к/проекту/SalesAggregator
   ```

3. **Сделайте скрипт исполняемым (только при первом запуске):**
   ```bash
   chmod +x gradlew
   chmod +x Запуск.command
   ```

4. **Выполните сборку проекта:**
   ```bash
   ./gradlew build
   ```

5. **Запустите приложение:**
   ```bash
   ./gradlew run
   ```

#### ✨ Альтернативный способ (один клик):
Дважды щелкните на файл **`Запуск.command`** в корне проекта!

---

### Вариант 3: Запуск через IntelliJ IDEA (рекомендуется)

#### Шаги запуска:

1. **Откройте проект в IntelliJ IDEA**
   - `File → Open...` → выберите папку `SalesAggregator`
   - IDEA автоматически определит Gradle-проект и загрузит зависимости

2. **Дождитесь индексации и загрузки зависимостей**
   - В нижней части окна отобразится прогресс-бар
   - Это может занять 1-2 минуты при первом запуске

3. **Найдите главный класс**
   - Откройте `src/main/java/com/salesaggregator/Main.java`
   - Или используйте поиск: `Shift + Shift`, введите "Main"

4. **Запустите приложение**
   - Нажмите зеленую кнопку ▶️ (Run) рядом с методом `main()`
   - Или используйте сочетание клавиш `Shift + F10`
   - Или через меню: `Run → Run 'Main'`

5. **Просмотрите результаты**
   - Вывод программы появится в окне "Run" внизу экрана

---

## 📤 Ожидаемый вывод программы

При успешном запуске программа:

1. **Загружает конфигурацию** из файла `app.properties`
2. **Читает CSV-данные** из файла `data/sales.csv`
3. **Обрабатывает и агрегирует данные** о продажах
4. **Выводит результаты** в консоль

### Пример вывода:

```
=== SalesAggregator v1.0 ===
Загрузка конфигурации из: app.properties
Чтение данных из: data/sales.csv

Обработано записей: 150
Общая сумма продаж: 1,234,567.89 руб.

Агрегация по категориям:
  - Электроника: 456,789.00 руб. (37%)
  - Одежда: 234,567.00 руб. (19%)
  - Продукты питания: 543,211.89 руб. (44%)

Топ-5 продуктов:
  1. Ноутбук — 123,456.00 руб.
  2. Смартфон — 98,765.00 руб.
  3. Телевизор — 87,654.00 руб.
  4. Наушники — 45,678.00 руб.
  5. Клавиатура — 23,456.00 руб.

Анализ завершен успешно!
```

---

## 🎯 Быстрый запуск (One-Click Launch)

Для максимального удобства в проекте предусмотрены скрипты быстрого запуска:

### Windows: `Запуск.bat`

Дважды щелкните по файлу `Запуск.bat` в корне проекта. Скрипт автоматически:
- Соберет проект
- Запустит приложение
- Отобразит результаты
- Дождется нажатия клавиши перед закрытием окна

### macOS/Linux: `Запуск.command`

Дважды щелкните по файлу `Запуск.command` в корне проекта. При первом запуске может потребоваться:
- Разрешить выполнение: `chmod +x Запуск.command`
- Разрешить запуск в настройках безопасности (macOS)

---

## 🔧 Устранение неполадок (Troubleshooting)

### ❌ Проблема: "Java не найдена" / "'java' is not recognized"

**Решение:**
1. Убедитесь, что Java JDK установлена: [Скачать Oracle JDK](https://www.oracle.com/java/technologies/downloads/)
2. Проверьте переменную окружения `JAVA_HOME`:
   - **Windows:** `echo %JAVA_HOME%`
   - **macOS/Linux:** `echo $JAVA_HOME`
3. Должна указывать на директорию JDK (например, `C:\Program Files\Java\jdk-17`)

### ❌ Проблема: "Permission denied" при запуске ./gradlew

**Решение (macOS/Linux):**
```bash
chmod +x gradlew
./gradlew build
```

### ❌ Проблема: Файл app.properties или sales.csv не найден

**Решение:**
1. Убедитесь, что файлы существуют в указанных местах:
   - `app.properties` — в корне проекта
   - `data/sales.csv` — в папке `data`
2. Проверьте правильность путей в коде
3. Запускайте приложение из корневой директории проекта

### ❌ Проблема: Gradle build fails / Ошибка сборки

**Решение:**
1. Очистите кэш Gradle:
   ```bash
   # Windows
   gradlew.bat clean
   
   # macOS/Linux
   ./gradlew clean
   ```
2. Удалите папку `.gradle` и повторите сборку
3. Проверьте подключение к интернету (для загрузки зависимостей)

### ❌ Проблема: "Encoding problems" / Проблемы с кодировкой

**Решение:**
1. Убедитесь, что CSV-файл сохранен в кодировке UTF-8
2. Добавьте в `build.gradle`:
   ```gradle
   tasks.withType(JavaCompile) {
       options.encoding = 'UTF-8'
   }
   ```

### ❌ Проблема: IntelliJ IDEA не видит Gradle проект

**Решение:**
1. `File → Invalidate Caches / Restart...`
2. Правой кнопкой на `build.gradle` → `Import Gradle Project`
3. `View → Tool Windows → Gradle` → нажмите кнопку обновления (🔄)

### ❌ Проблема: Медленная работа Gradle

**Решение:**
1. Увеличьте память для Gradle в файле `gradle.properties`:
   ```properties
   org.gradle.jvmargs=-Xmx2048m
   org.gradle.daemon=true
   org.gradle.parallel=true
   ```

---

## 📚 Дополнительные команды Gradle

```bash
# Сборка проекта без запуска
./gradlew build          # или gradlew.bat build на Windows

# Очистка собранных файлов
./gradlew clean

# Запуск приложения
./gradlew run

# Запуск тестов (если есть)
./gradlew test

# Просмотр всех доступных задач
./gradlew tasks

# Полная пересборка (очистка + сборка)
./gradlew clean build
```

---

## 📝 Примечания

- **CSV-файл (`data/sales.csv`)** должен содержать данные в формате:
  ```csv
  date,product,category,quantity,price,total
  2024-01-15,Ноутбук,Электроника,1,50000.00,50000.00
  2024-01-16,Молоко,Продукты питания,5,80.00,400.00
  ```

- **Конфигурационный файл (`app.properties`)** может содержать:
  ```properties
  input.file=data/sales.csv
  output.format=console
  encoding=UTF-8
  date.format=yyyy-MM-dd
  ```

---

## 🎓 Полезные ссылки

- [Документация Java](https://docs.oracle.com/en/java/)
- [Gradle User Guide](https://docs.gradle.org/current/userguide/userguide.html)
- [IntelliJ IDEA Documentation](https://www.jetbrains.com/help/idea/)

---

## 💪 Мотивация

Поздравляем! Вы создали полнофункциональное приложение для агрегации данных о продажах. 

Это важный навык в современной разработке — умение работать с данными, обрабатывать CSV-файлы и создавать аналитические инструменты востребовано во многих компаниях.

**Вы молодец!** 🎉 Продолжайте в том же духе и не останавливайтесь на достигнутом!

### Что дальше?

- 🚀 Добавьте экспорт результатов в Excel или JSON
- 📊 Создайте графическую визуализацию данных
- 🌐 Разработайте веб-интерфейс для загрузки CSV-файлов
- 🤖 Внедрите машинное обучение для прогнозирования продаж
- ☁️ Разверните приложение в облаке (AWS, Azure, Google Cloud)

**Помните:** каждый эксперт когда-то был новичком. Ваш путь только начинается! 💻✨

---

## 📞 Контакты и поддержка

Если у вас возникли вопросы или проблемы:
1. Внимательно прочитайте раздел "Устранение неполадок"
2. Проверьте логи и сообщения об ошибках
3. Обратитесь к преподавателю или однокурсникам

**Удачи в выполнении лабораторной работы!** 🍀

---

<div align="center">
  
**Made with ❤️ for learning Java and Data Processing**

⭐️ Если этот проект помог вам, поставьте звезду на GitHub!

</div>
